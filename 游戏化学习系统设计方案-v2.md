# 游戏化学习系统·沉浸式重构方案 v2

> 版本：v2.0 | 日期：2026-06-27 | 状态：设计阶段，待实施
> 基于 v1.0（游戏化宠物养成系统设计方案，2026-04-20）的全面重构升级
> 融合豆包沉浸式方案与 Qwen 3.7 设计方案的核心思想，结合具体用户需求定制

---

## 一、问题诊断：旧版为什么不吸引孩子？

AI 生成的 v1 版本踩了所有"学习游戏化"的大忌，也是绝大多数鸡肋学习 App 的通病：

### 1. 只有外壳，没有情绪
宠物是静态 SVG 贴图。虽有 mood 参数（idle/happy/excited/hurt），但缺乏**持续陪伴感**——孩子不会觉得这是"伙伴"，只是一个随答题切换表情的图标。无性格、无记忆、无专属互动。

### 2. 只有任务，没有体验
关卡是"读文本 + 做题"的传统模式套壳。冒险路径本质上是一条时间轴，孩子**被动阅读**而非**主动探索操作**。对比真正的游戏（如蛋仔派对、原神），每一步都有操作、有反馈、有画面。

### 3. 只有结果奖励，没有即时爽感
短视频是 0.5 秒即时反馈，游戏是每一步都有音效/动画/震动。而旧方案只在答完一套题后给一个结算页——中途全程枯燥。注意：现有冒险模式引入了 HP/Combo/Boss 等概念，但孩子对"扣血"可能产生挫败感而非兴奋感。

### 4. 无差异养成
所有孩子的宠物、关卡、剧情一模一样，没有"我的宠物独一无二"的执念。缺少收集欲、炫耀欲、专属感的驱动。

### 核心误判
旧方案的底层逻辑是"学习任务附带游戏外壳"。正确的逻辑应该是：**"游戏过程内嵌学习"**——孩子是为了养宠物、闯关、解锁而主动学习，而不是为了学习被迫玩游戏。

---

## 二、设计原则

| 原则 | 说明 |
|------|------|
| **游戏内嵌学习** | 不是"答对题给奖励"，而是"闯关必须用到知识"，知识是游戏的"操作方式" |
| **短视频式节奏** | 单关卡 60-120 秒，3 秒开场 → 互动挑战 → 5 秒反馈 → 无缝下一关 |
| **毫秒级即时反馈** | 每一步操作（点击/拖拽/连线）都有视觉+音效反馈，拒绝"做完才结算" |
| **弱化惩罚，强化陪伴** | 去除 HP 扣血/嘲讽。错题 → 宠物温柔鼓励 → 引导重试。用"损失厌恶"（宠物休眠）替代"惩罚" |
| **专属养成感** | 宠物性格、外观、场景均为孩子独有，通过学习积累变化，可炫耀 |
| **可扩展架构** | 先服务于自家一年级娃，但保留扩展为多人产品的数据库/API 设计 |

---

## 三、情感宠物系统（核心亮点）

### 3.1 视觉风格：Q版萌系（可升级为 Live2D）

采用 Q版萌系画风（大眼、圆脸、小身体），对一年级孩子最亲切。当前用 SVG + CSS 关键帧动画实现，后期可平滑升级为 Live2D，只需替换渲染层。

**升级路径：**
```
SVG + CSS 关键帧（当前 MVP）
  → 精灵序列帧 PNG（更细腻动作，可用 AI 生成差分图）
  → Live2D Cubism（呼吸/眨眼/视线跟随，最精致）
```

三大学科的精灵形象：
- 数学·智慧猫：戴眼镜的圆脸猫咪，尾巴随 combo 数摇摆
- 语文·小书童：手持书卷的学童，斗笠随等级升级
- 英语·小魔女：骑扫帚的小巫师，帽子尖随等级变长

### 3.2 多层级动态表情与动作

宠物**所有状态实时跟随学习行为变化**，全程动态，无静止画面：

| 状态 | 触发条件 | 动画 |
|------|---------|------|
| 待机 | 打开 App 后无操作 | 轻微眨眼、晃身体、偶尔蹦跳 |
| 迎接 | 孩子进入学习区 | 跑过来、挥手、转圈欢呼 + 轻快音效 |
| 答对 | 一道题正确 | 跳跃拍手、发光、比心、蹭屏幕边缘 |
| 连击 | 连续答对 3+ 题 | 旋转跳跃、火焰流光、专属喝彩台词 |
| 答错 | 一道题错误 | 低头委屈、耷拉耳朵、软萌鼓励台词 |
| 进化 | 等级提升 | 星光蜕变动画 + 专属成长 CG |
| 休眠 | 连续 3 天未学 | 颜色变暗、趴下打鼾、偶尔翻身 |

### 3.3 专属性格与记忆

**4 种开局可选性格：**
- 元气活泼型：台词轻快热闹，答对时超兴奋
- 温柔治愈型：软萌鼓励，错题时特别暖心
- 傲娇呆萌型：嘴硬心软，"哼，这题我本来不想帮你的"
- 勇敢冒险型：热血台词，"我们一起打败难题！"

**记忆式交互：** 记录孩子薄弱知识点（如"凑十法还不太熟练"）、学习习惯（"你周六总偷懒哦"），针对性鼓励，形成专属陪伴感。

### 3.4 损失厌恶机制（星灵蛋）

借鉴 Qwen 方案的设计巧思，替代传统的"惩罚"：

- **设定**：孩子偶然捡到了一颗"星灵蛋"。星灵的能量来源于"知识光波"（学习行为）
- **喂养**：每天学习 = 喂食星灵，它会成长、变亮、变成各种形态
- **休眠机制**：连续 3 天不学习 → 星灵颜色变暗、趴下打鼾 → 需要用"复习关卡"唤醒
- **心理效果**：孩子舍不得宠物休眠，"我去答题让宠物开心"比"我妈让我答题"有效得多

### 3.5 可视化养成体系

- **等级成长**：1-10 级幼体 → 成长体 → 完全体，外形、特效、动作随等级迭代
- **属性养成**：知识值（答题正确率）、专注力（连续学习天数）、勇气值（挑战高难度关卡）
- **外观收集**：皮肤、配饰、专属光环，全部通过学习闯关解锁，无氪金门槛
- **专属场景**：宠物小屋可通过学习奖励装饰（沙发、小床、小夜灯），打造独一无二的专属空间
- **永久装饰**：连续 7 天打卡 → 宠物脖子上多一条"毅力小围巾"，永久保留，可向同学炫耀

---

## 四、沉浸式关卡系统（告别时间轴文本弹窗）

### 4.1 关卡形态转变

**旧（❌）：** 文本弹窗讲故事 → 选择题 → 下一题 → 结算页
**新（✅）：** 探索场景 → 触发挑战 → 操作式答题 → 即时反馈 → 无缝下一关

### 4.2 统一关卡节奏（对标短视频）

```
[3s 开场]  宠物动态小动画，快速带入场景（如"我饿了！帮我收集能量块！"）
[60-120s]  场景化互动挑战（拖拽/点击/连线/描红，拒绝纯文字选题）
[5s 反馈]  通关动画 + 宠物欢呼 + 道具掉落 + 能量上升
[自动]     无缝衔接下一关，和刷短视频一样无需手动切换
```

### 4.3 数学关卡设计（MVP 优先）

| 知识点 | 场景 | 玩法 |
|--------|------|------|
| 凑十法 | 宠物饿了，面前有"能量合成炉" | 拖拽苹果到碗里，凑满 10 个合成一块能量块。猫跳起来吃掉，欢呼。8 + ? = 10 → 拖 2 个进碗 |
| 20以内加减 | 打地鼠式 | 屏幕上冒出数字地鼠，孩子点击正确结果的地鼠。越快 combo 越高 |
| 认识图形 | 拼图工坊 | 拖拽圆形/三角形/正方形拼出房子、树、车。拼对 → 宠物鼓掌 |
| 认识时间 | 宠物的一天 | 拖动时针到指定时间——"7:00 宠物该起床啦"、"12:00 该吃饭啦"。对 → 宠物做对应动作 |
| 认识人民币 | 宠物商店 | 用纸币硬币（拖拽）组合出指定金额，买零食/玩具给宠物。买对 → 宠物开心享用 |

### 4.4 语文关卡设计（拼音 + 识字）

| 模块 | 场景 | 玩法 |
|------|------|------|
| 拼音·听音识字母 | 字母泡泡 | 听到发音（b~），点击泡泡中正确的字母。点对 → 泡泡炸开，宠物追泡泡 |
| 拼音·声调练习 | 声调彩虹桥 | 给音节拖拽正确的声调（ā/á/ǎ/à）。拖对 → 桥亮，宠物过桥 |
| 识字·翻牌配对 | 记忆花园 | 翻牌找相同汉字，配对成功收集到图鉴。配对越多，图鉴越满 |
| 识字·组字寻宝 | 汉字工坊 | 拖拽偏旁部首拼接成字（如亻+门=们）。拼对 → 字发光，宠物兴奋 |

### 4.5 V2 预留模块（不在 MVP 范围）

笔顺描红（跟着光点描笔画）、词语接龙、成语故事拼图、看图作文（需 AI 辅助）——这些在第一阶段验证有效后再补。

---

## 五、奖励与留存体系

### 5.1 三层奖励金字塔

| 层级 | 触发时机 | 内容 | 作用 |
|------|---------|------|------|
| **毫秒级** | 每一次操作 | 点击特效、宠物微表情、音效、光点掉落 | 像短视频一样每一次都有反馈 |
| **短期（当日）** | 通过关卡/完成每日任务 | 宠物零食、专属配饰、场景装饰、能量增长 | 当天努力当天看得见成果 |
| **长期（周/月）** | 累计成就 | 专属勋章、新地图解锁、宠物进化、永久装饰 | 持续学习的核心粘性 |

### 5.2 盲盒扭蛋系统

学习获得的金币用于"星际扭蛋机"抽取**随机**配饰（帽子、眼镜、围巾）和栖息地家具。**收集癖和随机性是极强的驱动力**——孩子为了凑齐一套装备，会主动多学。

### 5.3 成就图鉴

三本可翻阅的图鉴：
- **知识点图鉴**：学过的所有知识点，点亮一个添加一页，未学的是灰色
- **闯关图鉴**：通关记录、最高 Combo、最快速度
- **宠物成长图鉴**：宠物从蛋 → 幼体 → 成长体 → 完全体的所有形态记录

### 5.4 现实虚拟联动（V2）

现实中完成一篇作文 → 系统中掉落"传说级羽毛" → 可用来给宠物进化翅膀。把"额外学习"变成"隐藏副本奖励"。

---

## 六、双路径技术方案

### 6.1 共享后端（Spring Boot 3.2.4）

两条路径**共用同一个 Spring Boot 后端**，后端改动最小化：

**扩展现有 API：**
- `/api/v1/quiz/**` —— 题库接口（重做内容，保留结构）
- `/api/v1/quiz/subjects/{subject}/nodes` —— 返回知识点树（按一年级数学/拼音/识字重建）
- `/api/v1/quiz/session/**` —— 会话管理（保留，场景化关卡复用同一会话模型）
- `/api/v1/spirit/**` —— 精灵养成接口（扩展性格、情绪历史、外观收集字段）
- `/api/v1/shop/**` —— 扭蛋/商店接口（扩增扭蛋逻辑）
- `/api/v1/achievement/**` —— 成就接口（扩增永久装饰触发逻辑）

**数据库扩展（在现有 28 张表基础上）：**
- `spirit_species` 表 → 增加性格字段、情绪历史、外观收集 JSON
- `item_def` 表 → 增加"永久装饰"标记字段
- 新增 `gacha_history` 表 → 扭蛋抽取记录
- `data.sql` → **全部重做**：一学年数学题库 + 拼音题库 + 识字题库

**现有 `data.sql` 问题：数学题严重超纲（25×4、圆周长 2πr），全部需要替换为一年级水平内容。**

### 6.2 路径 A：Svelte 原地进化

在现有 `frontend/` 目录上改造，最大化复用已有成果。

**保留部分：**
- `lib/stores/auth.svelte.ts` —— 认证状态
- `lib/stores/energy.svelte.ts` —— 能量系统
- `lib/stores/spirit.svelte.ts` —— 精灵状态
- `lib/api/client.ts` —— HTTP 客户端封装
- `lib/components/feedback/` —— 即时反馈组件（ParticleEffect、CelebrationOverlay）
- `routes/(auth)/` —— 登录注册页面

**改造/重做部分：**

| 文件 | 改动 |
|------|------|
| `lib/components/spirit/SpiritAvatar.svelte` | **大幅改造**：增加性格台词、更多情绪动画、Q版萌系画风、损失厌恶视觉效果 |
| `lib/components/spirit/PersonalityRadar.svelte` | 配合性格系统调整 |
| `routes/app/study/[subject]/explore/+page.svelte` | **重写**：去掉 HP/Boss/时间轴，改为场景化操作 + 即时反馈 |
| `lib/components/study/AdventurePath.svelte` | **弃用**（时间轴概念不再保留） |
| `lib/components/study/HpBar.svelte` | **弃用**（去除惩罚性机制） |
| `lib/components/study/BossFight.svelte` | **弃用** |
| `lib/components/study/ComboCounter.svelte` | **保留改造**（combo 仍是正向激励） |
| **新增：场景组件** | `SceneMathTen.svelte`、`ScenePinyinBubble.svelte`、`SceneCharacterMatch.svelte` 等 |
| `routes/app/spirit/+page.svelte` | 增强宠物互动（戳/摸反馈）、展示永久装饰 |

### 6.3 路径 B：Godot 前端

在 Windows 端新建 Godot 4.7 项目，通过 HTTP 调用 WSL 中的 Spring 后端。

**项目路径：** `C:\Users\fxb_2\ZCodeProject\pet-growup-godot\`

**Godot 项目结构：**
```
pet-growup-godot/
├── project.godot               # Godot 项目配置
├── scenes/
│   ├── main_menu.tscn          # 主菜单（登录/选宠/主页）
│   ├── pet_room.tscn           # 宠物小屋（喂养/互动/装饰）
│   ├── math/
│   │   ├── ten_make.tscn       # 凑十法关卡
│   │   ├── arithmetic.tscn     # 20以内加减
│   │   ├── shapes.tscn         # 认识图形
│   │   └── clock.tscn          # 认识时间
│   └── chinese/
│       ├── pinyin_bubble.tscn  # 拼音泡泡
│       └── character_match.tscn # 识字翻牌
├── scripts/
│   ├── api_client.gd           # HTTP API 客户端（封装对 Spring 后端的调用）
│   ├── pet_controller.gd       # 宠物动画状态机
│   ├── session_manager.gd      # 答题会话管理
│   └── audio_manager.gd        # 音效管理
├── assets/
│   ├── sprites/pet/            # 宠物精灵动画帧
│   ├── sprites/ui/             # UI 素材
│   └── audio/                  # 音效/背景音乐
└── shaders/                    # 粒子/发光特效
```

**Godot 与 Spring 后端通信方式：**
- 使用 Godot 内置 `HTTPRequest` 节点调用 `http://localhost:8080/api/v1/**`
- 认证：登录后存储 JWT token，每次请求带 `Authorization: Bearer <token>`
- WebSocket 实时反馈：Godot 用 `WebSocketPeer` 连接后端的 STOMP 端点

**Godot 导出目标：** HTML5（WebAssembly）在浏览器运行；未来可选导出 Android/iOS。

### 6.4 双路径对比场景

两条路径用**同一个场景**公平对比：**数学·凑十法**

| 维度 | 路径 A (Svelte) | 路径 B (Godot) |
|------|-----|-----|
| 宠物动画 | CSS 关键帧摆动 + emoji 表情过渡 | AnimatedSprite2D 骨骼动画 |
| 拖拽交互 | pointer events + Svelte 绑定 | 内置 Area2D + Drag 物理 |
| 粒子特效 | CSS animation + SVG sparkles | GPU Particle2D + Shader |
| 音效 | Web Audio API | 内置 AudioStreamPlayer |
| 开发速度 | 快（复用现有组件） | 中（需建项目+写 GDScript） |
| 沉浸感上限 | 中等（可升 Live2D） | 高 |
| 未来手机端 | 需 PWA 套壳 | 一键导出 |

**决策点：** 两条路实现完"凑十法"后，请娃（目标用户）试玩，观察：
- 哪个宠物的反应让他**笑出来**？
- 哪个关卡的操作让他**主动想再玩一次**？
- 哪个的视觉效果让他喊**"哇"**？

根据观察结果选一条路径深耕。

---

## 七、落地路线图

### Sprint 1（2-3 周）：后端准备 + 宠物 MVP + 对比验证

**目标：** 双路径各做出"凑十法"可玩 Demo，给娃试玩对比。

| 任务 | 路径 | 说明 |
|------|------|------|
| 重做 data.sql 数学题库 | 共用 | 替换超纲题，写入一年级数学内容 |
| 扩展后端 API | 共用 | 增加性格/扭蛋/永久装饰字段，不改核心逻辑 |
| 路径A：改造 SpiritAvatar | Svelte | Q版萌系 + 4情绪动画 + 性格台词 |
| 路径A：新场景组件 | Svelte | SceneMathTen.svelte（凑十法） |
| 路径B：建 Godot 项目 | Godot | 项目结构 + API 客户端 |
| 路径B：宠物场景 | Godot | pet_room.tscn（待机/欢迎/答对/答错） |
| 路径B：凑十法关卡 | Godot | ten_make.tscn（拖拽苹果到碗） |
| **验证节点 🔍** | — | **娃试玩 "凑十法" 两个版本 → 选一条路径** |

### Sprint 2（2-3 周）：数学全关卡 + 语文拼音识字

**目标：** 在选定的路径上，把数学关卡做完，拼音识字上线。

| 任务 | 说明 |
|------|------|
| 数学：20以内加减关卡 | 打地鼠 / 限时轻挑战 |
| 数学：认识图形关卡 | 拼图工坊 |
| 数学：认识时间关卡 | 宠物的一天 |
| 数学：认识人民币关卡 | 宠物商店 |
| 语文：拼音·听音识字母 | 字母泡泡 |
| 语文：拼音·声调练习 | 声调彩虹桥 |
| 语文：识字·翻牌配对 | 记忆花园 |
| 语文：识字·组字寻宝 | 汉字工坊 |

### Sprint 3（1-2 周）：奖励体系 + 留存闭环

| 任务 | 说明 |
|------|------|
| 三层奖励上线 | 即时反馈微动效 + 每日奖励 + 成就徽章 |
| 扭蛋系统 | 随机配饰抽取 |
| 永久装饰 | 毅力围巾等持续激励 |
| 成就图鉴 | 三本图鉴 UI |
| **V2 模块设计** | 笔顺/组词/成语/看图作文（仅设计，不编码） |

---

## 附录：关键参考来源

本方案融合了以下文档的核心思想：

- **游戏化宠物养成系统设计方案 v1.0**（2026-04-20）—— 系统架构基础，"一核三柱两环"
- **冒险模式设计方案** —— 已实现的答题 → 冒险改造，HP/Combo/Boss 体系
- **豆包·沉浸式重构完整方案** —— "游戏内嵌学习"核心主张、短视频节奏拆解、情感宠物多层级设计、4阶段落地优先级
- **Qwen 3.7·游戏化学习系统设计方案** —— 星灵蛋损失厌恶、乘法合成炉具象化、盲盒扭蛋、永久奖状、MVP 验证标准
- **一年级课程标准**（人教版，2024）—— 数学：20以内加减、凑十法、认识图形/钟表/人民币。语文：拼音（声母/韵母/声调）、识字（300 字）

> 方案 v2.0 编写于 2026-06-27。当前项目已实现 P0/P1 全部子系统（11 个后端模块、20 张数据表，代码见仓库 `main` 分支）。
> v2 核心使命：在现有技术资产之上，重构"游戏体验层"，让宠物有灵魂、关卡能动手、反馈零延迟。
