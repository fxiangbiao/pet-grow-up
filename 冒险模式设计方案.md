# 冒险模式 — 游戏化学习改造方案

## 核心设计理念

**把「答题」变成「冒险」，而不是在答题外面套 UI。**

每次学习 = 一次小冒险。精灵和你一起踏上一条冒险路径，每道题是路径上的一个「遭遇战」。对错直接影响冒险进程，而不是冷冰冰地跳到下一题。

---

## 游戏机制

### 1. 冒险路径 (Adventure Path)

```
[起点] ─── 🗡 题1 ─── 🗡 题2 ─── 🗡 题3 ─── 🗡 题4 ─── 👑 Boss ─── [终点]
           ↑           ↑           ↑           ↑           ↑
        路径节点    路径节点    路径节点    路径节点    Boss 节点
```

- 顶部显示一条水平路径，节点代表题目
- 当前题目高亮闪烁，已完成题目显示 ✅/❌
- 前方未达节点显示 🔒
- 精灵头像沿着路径移动（当前在第几题，精灵就在哪个节点）

### 2. HP 系统

| 机制 | 说明 |
|------|------|
| 初始 HP | 5❤️ |
| 答错扣血 | 常规题 -1❤️，Boss 题 -2❤️ |
| HP 归零 | 冒险提前结束（已答对的题仍计分） |
| HP 恢复 | 每答对 2 题，宝箱中可恢复 1❤️（可选） |

**显示方式：** 路径上方的心形条，每失去一颗心变灰+破碎动画。

### 3. Combo 连击系统

| Combo | 条件 | 倍率 |
|-------|------|------|
| ×1 | 默认 | 1.0× |
| ×1.5 | 连续答对 2 题 | 1.5× |
| ×2 | 连续答对 4 题 | 2.0× |
| 重置 | 答错一题 | 回到 ×1 |

**显示方式：** 答题区上方显眼的 Combo 计数器，连击时有火焰/流光动画。

### 4. 宝箱奖励 (Treasure Chest)

每连续答对 2 题，路径上出现宝箱动画：
- 第 2 题答对 → 宝箱打开 🎁 → 额外 +5 能量
- 第 4 题答对 → 大宝箱 🏆 → 额外 +10 能量 + 回 1❤️

### 5. Boss 战

| 机制 | 说明 |
|------|------|
| 最后一题 | 自动变为 Boss 战 |
| Boss 血条 | 答题前显示 Boss 头像+血条（模拟血量） |
| 答对 | 对 Boss 造成伤害（血条减少动画） |
| 答错 | Boss 反击，扣 2❤️ |
| Boss 击败 | 额外 +20 能量奖励 |
| Boss 未被击败 | 仍以普通题结算 |

### 6. 冒险结算增强

结算页面增加冒险数据：
- 冒险结果：通关/中途倒下 🏁/💀
- 最高 Combo：×N
- Boss 击败：✅/❌
- 宝箱收集：N/2 个
- HP 剩余：N/5
- Combo 能量加成：额外 +N 点

---

## 视觉与交互

### 主题差异化

| 学科 | 路径风格 | 精灵移动方式 | Boss 形象 | 色彩 |
|------|---------|-------------|-----------|------|
| 诗词大陆 | 水墨山水卷轴 | 御剑飞行 | 墨龙/黑暗诗魔 | 琥珀+墨色 |
| 智慧王国 | 几何水晶路径 | 踩方块弹跳 | 混沌几何体 | 蓝+青 |
| 魔法学院 | 星光轨迹 | 骑扫帚飞行 | 暗影巫师 | 紫+粉 |

### 关键动画

| 事件 | 动画 |
|------|------|
| 答对 | 绿色闪光 + 节点变 ✅ + 精灵跳跃 |
| 答错 | 红色震动 + 节点变 ❌ + 精灵受伤晃动 + 屏幕微抖 |
| Combo 增加 | 计数器放大脉冲 + 火焰特效 |
| 宝箱 | 宝箱从路径升起 → 打开 → 金光飞向能量计数器 |
| Boss 受伤 | Boss 血条减少 + 伤害数字 |
| HP 减少 | 心形碎裂 + 变灰 |
| 冒险结束 | 慢动作 + 结算过渡 |

---

## 技术实现方案

### 总原则

**尽可能在前端完成，后端只做最小修改。**

Combo 计算、HP 管理、宝箱触发、Boss 标记全部由前端维护状态。后端只需要：
1. 返回值里带上 `isLastQuestion` 标记（用于判断 Boss）
2. 结算接口返回 combo 奖励能量

### 后端修改

| 文件 | 修改内容 |
|------|---------|
| `AnswerResultDTO` | 新增 `isLastQuestion: boolean` 字段 |
| `SessionResultDTO` | 新增 `maxCombo: int`, `bossDefeated: boolean`, `comboBonusEnergy: long` |
| `ExplorationService.completeSession()` | combo 奖励 = maxCombo > 1 ? (maxCombo - 1) × 10 : 0 |
| `ExplorationService.submitAnswer()` | 标记 `isLastQuestion` |

### 前端修改

**主要改造文件：** `explore/+page.svelte`（大幅重写）

**新增状态变量：**
```typescript
let hp = $state(5);
let combo = $state(0);
let maxCombo = $state(0);
let bossDefeated = $state(false);
let treasureFound = $state(0);
```

**新增组件：**

| 文件 | 说明 |
|------|------|
| `components/study/AdventurePath.svelte` | 冒险路径（节点+精灵位置） |
| `components/study/ComboCounter.svelte` | Combo 显示+动画 |
| `components/study/BossFight.svelte` | Boss 战 UI（血条+头像） |
| `components/study/HpBar.svelte` | HP 心形条 |
| `components/study/TreasureChest.svelte` | 宝箱开启动画 |

**改造后 playing 阶段布局：**
```
┌──────────────────────────────────┐
│  ❤️❤️❤️❤️🖤  HP: 4/5    ×2 COMBO!   │  ← HP + Combo
│  ─── 🗡✅ ─── 🗡❌ ─── 🗡🔥 ─── 🔒 ─── 👑  │  ← 冒险路径
│  🐱 精灵正站在第 3 题位置             │
│  ─────────────────────────────── │
│  📖 题目文本                      │
│  ○ 选项 A                        │
│  ● 选项 B                        │
│  ○ 选项 C                        │
│  [提交答案]                       │
│  ✅/❌ 反馈                       │
└──────────────────────────────────┘
```

### 改造后 confirm 阶段

**新增冒险信息卡：**
```
┌──────────────────────────────────┐
│  📜 诗词大陆 · 诗词入门           │
│  🐱 精灵伙伴与你同行               │
│  ─────────────────────────────── │
│  🗡 5 场遭遇战                    │
│  👑 最终 Boss 战                 │
│  💰 宝箱奖励: 每连对 2 题触发     │
│  ❤️ 初始 HP: 5，答错扣 1          │
│  🔥 连击倍率: ×1 → ×1.5 → ×2    │
│  [开始冒险！]                     │
└──────────────────────────────────┘
```

---

## 实施步骤

| 步骤 | 内容 | 难度 |
|------|------|------|
| 1 | 新增后端 DTO 字段（AnswerResultDTO + SessionResultDTO） | 低 |
| 2 | 后端 combo 奖励计算 | 低 |
| 3 | HpBar、ComboCounter 两个小组件 | 低 |
| 4 | AdventurePath 路径组件 | 中 |
| 5 | BossFight 组件 | 中 |
| 6 | TreasureChest 宝箱动画 | 中 |
| 7 | 改造 confirm 阶段 UI | 低 |
| 8 | 改造 playing 阶段整合所有新组件 | 高 |
| 9 | 改造 handleSubmit 逻辑（combo/HP/宝箱/Boss） | 中 |
| 10 | SessionResult 增强显示 | 低 |
| 11 | 动画调试打磨 | 中 |

---

## 为什么不改后端太多？

1. **Combo 是纯前端概念** — 后端不需要知道当前连击数，只需要知道最终最高连击
2. **HP 也是纯前端概念** — 不影响分数计算，只影响冒险是否提前结束
3. **宝箱是即时反馈** — 后端无需记录，前端触发后直接加能量显示
4. **减少风险** — 不改动核心答题流程，避免引入 bug

唯一后端需要知道的：**Boss 被击败了吗？最高 Combo 是多少？** 用于结算时给奖励。
